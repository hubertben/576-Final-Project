using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class weapon_animator_controller : MonoBehaviour
{
    //weapon anims
    public AnimationClip SwingSword;

    // Update is called once per frame
    void Update()
    {
        if (Input.GetMouseButtonDown(0))
        {
        }
    }
}
